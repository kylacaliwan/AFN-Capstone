# Pages

Route-level screens are grouped by user workspace:

- `admin/` for admin and superadmin operations.
- `client/` for customer-facing service flows.
- `technician/` for field technician workflows.
- `follow_up/` for after-sales case handling.

Authentication pages live at this level because they are shared by every role.

# Components

Shared React components are grouped by role:

- `layout/` contains app chrome such as sidebar, topbar, and page frame.
- `ui/` contains small reusable display components such as badges, cards, and indicators.
- `shared/` contains domain components reused across more than one page area.

Page-specific components should live beside the page or in a feature folder, not here.
